# Enterprise RAG V2.0 迭代进度报告

> **文档版本**：v1.1  
> **生成日期**：2026-03-01  
> **适用范围**：V2.0 检索与质量升级（L1.8 → L2.5）  
> **关联文档**：`V2.0_PRD_检索与质量.md`、`V2.0_TRD_检索与质量.md`、`RAG系统能力三级模型_L1_L2_L3.md`、`docs/V2.0_检索与配置汇总.md`、`开发全流程.md`

---

## 一、报告目的与适用范围

本报告用于**清晰呈现项目当前迭代进度**，包括：

- **版本路线**：V1 → V2.0 → V2.1 → V2.2 与能力等级（L1.8 → L2.5 → L2.7 → L2.9）
- **V2.0 设计完备度**：按四大领域、20 个开发模块对照 TRD 的覆盖情况
- **实现进度**：骨架、数据库、API、前端、测试及已完成的修复项
- **缺口与待办**：TRD 待补项、横切串联、前端联动等，并标注优先级

便于产品、开发、测试一眼看清**整体进度**与**下一步工作**。

---

## 二、版本路线与当前目标

| 版本   | 能力等级 | 定位           | 状态     |
|--------|----------|----------------|----------|
| V1     | L1.8     | 基础级上限     | 已上线   |
| **V2.0** | **L2.5** | 检索质变 + 质量保障 | **进行中** |
| V2.1   | L2.7     | 智能与企业级   | 计划中   |
| V2.2   | L2.9     | 精细化与运营   | 计划中   |

**V2.0 核心目标**（8 周：B1 Week 1–3 + B2 Week 4–6 + 集成验证）：

1. **检索质量质变**：单路向量 → BM25 + 向量混合 + RRF 融合 + 父文档检索 + 自适应深度
2. **答案可信度保障**：无质量验证 → NLI 幻觉检测 + 置信度评估 + 智能拒答 + 引用验证 + 用户反馈（点赞/点踩）

---

## 三、V2.0 四大领域 × 20 模块进度表

以下按 **TRD 覆盖情况** 与 **实现状态** 分模块列出。  
「TRD 覆盖」表示《V2.0_TRD_检索与质量》是否写全、写清；「实现状态」表示当前代码与迁移的完成情况。

### 3.1 领域一：基础设施（Infrastructure）

| 模块编号 | 模块名称           | TRD 覆盖 | 缺口简述           | 实现状态 | 优先级 |
|----------|--------------------|----------|--------------------|----------|--------|
| I-1      | PostgreSQL + pg_jieba | ✅ 完备 | —                  | ✅ 迁移 V2.0_001，镜像 sdrzlyz/pg-ts-jieba:16 | P0 |
| I-2      | chunks 表与 FTS/tsvector | ✅ 完备 | —                  | ✅ 迁移 V2.0_002、V2.0_003、V2.0_007 | P0 |
| I-3      | 配置与 .env        | ✅ 完备 | —                  | ✅ .env 已升级 V2.0 版本说明 | P0 |

### 3.2 领域二：检索引擎（Retrieval）

| 模块编号 | 模块名称           | TRD 覆盖 | 缺口简述           | 实现状态 | 优先级 |
|----------|--------------------|----------|--------------------|----------|--------|
| R-1      | BM25 检索器        | ✅ 完备 | —                  | ✅ `app/rag/bm25_retriever.py` | P0 |
| R-2      | RRF 融合           | ✅ 完备 | —                  | ✅ `app/rag/rrf_fusion.py` | P0 |
| R-3      | 父文档检索         | ✅ 完备 | —                  | ✅ `app/rag/parent_retriever.py` | P0 |
| R-4      | 向量检索器设计     | ✅ 完备 | ~~接口统一需明确~~ → TRD 附录十九已补充 | ✅ 代码已有 `retriever.py`、`vector_store.py` | P1 |
| R-5      | 自适应检索深度     | ✅ 完备 | —                  | ✅ `app/rag/adaptive_topk.py` | P1 |
| R-6      | 检索结果去噪       | ✅ 完备 | —                  | ✅ `app/rag/denoiser.py` | P1 |
| R-7      | 分块（中文递归/Token/字符） | ✅ 完备 | —                  | ✅ `app/rag/chunker.py` | P0 |
| R-8      | 标题增强           | ✅ 完备 | —                  | ✅ `app/rag/title_extractor.py` | P1 |
| R-9      | 混合 Pipeline 编排 | ✅ 完备 | —                  | ✅ `app/rag/hybrid_pipeline.py`、`pipeline.py` | P0 |

### 3.3 领域三：质量保障（Verification）

| 模块编号 | 模块名称           | TRD 覆盖 | 缺口简述           | 实现状态 | 优先级 |
|----------|--------------------|----------|--------------------|----------|--------|
| V-1      | NLI 幻觉检测       | ✅ 完备 | —                  | ✅ `app/verify/nli_detector.py` | P0 |
| V-2      | 置信度评估         | ✅ 完备 | —                  | ✅ `app/verify/confidence_scorer.py` | P0 |
| V-3      | 答案验证 Pipeline  | ✅ 完备 | —                  | ✅ `app/verify/verify_pipeline.py` | P0 |
| V-4      | 智能拒答与引用验证 | ✅ 完备 | —                  | ✅ `app/verify/refusal.py`、`citation_verifier.py` | P0/P1 |
| V-5      | 反馈与 V1 迁移     | ✅ 完备 | ~~API 路径与表关系需写清~~ → TRD 附录十八已补充 | ✅ 迁移 V2.0_004，API 已存在 | P1 |

### 3.4 领域四：前端基础（Frontend）

| 模块编号 | 模块名称           | TRD 覆盖 | 缺口简述           | 实现状态 | 优先级 |
|----------|--------------------|----------|--------------------|----------|--------|
| F-1      | 流式问答与 UI 组件 | ✅ 完备 | —                  | ✅ `components/qa`、`use-stream-qa`、`stores/qa-store` | P0 |
| F-2      | 布局与页面结构     | ⚠️ 有缺口 | 布局组件清单、路由与 TRD 对应关系可再细化 | ✅ 骨架存在 | P2 |
| F-3      | API 与类型         | ✅ 完备 | ~~类型表需补充~~ → 已在 TRD 中补充 | ✅ `Strategy` 已含 top_k、expansion_enabled 等 | P1 |
| F-4      | 检索设置与策略联动 | ✅ 完备 | ~~交互与文案需明确~~ → TRD + 配置汇总已补充，代码已实现 | ✅ `QASettingsSheet.tsx` 已实现策略联动 | P1 |

---

## 四、TRD 横切项与缺口汇总

以下为**跨多模块**的设计与实现项，单独列出便于排期与补文档。

| 横切项 | 说明 | 优先级 | 状态 |
|--------|------|--------|------|
| **pipeline / qa_service 串联** | 从「用户提问」到「检索 → 验证 → 拒答/生成」的端到端伪代码或流程图 | **高** | ✅ TRD 附录十七已补充完整流程图 |
| **document_tasks 串联** | 文档解析、分块、写入 chunks、标题增强等在 Celery 任务中的调用顺序与配置传递 | 中 | ✅ TRD 4.16 节已有详细设计 |
| **kimi_provider 设计** | Kimi 仅支持 temperature=1.0、模型名与 .env 对应关系 | 低 | ✅ 已实现，TRD 可补文档 |
| **反馈与 V1 迁移** | 反馈 API 路径为 `POST /api/retrieval/feedback`；表为 `retrieval_feedbacks` 扩展 | 中 | ✅ TRD 附录十八已补充完整说明 |

---

## 五、实现进度小结（按层级）

| 层级 | 状态 | 说明 |
|------|------|------|
| 后端骨架 | ✅ | `app/rag`（BM25、RRF、父文档、去噪、自适应深度、chunker、title_extractor）、`app/verify`（NLI、置信度、引用验证、拒答）、`app/llm`（Kimi Provider） |
| 前端骨架 | ✅ | `components/qa`（ConfidenceBadge、RefusalAlert、FeedbackPanel）、`stores/qa-store`、`hooks/use-stream-qa` |
| 数据库迁移 | ✅ | V2.0_001～V2.0_007：pg_jieba、chunks 表、tsvector 触发器、retrieval_feedbacks 扩展、knowledge_bases 扩展、retrieval_logs 扩展、chunks 约束 |
| API | ✅ | `/api/qa/ask`、`/api/qa/stream` 支持 retrieval_mode、验证结果；反馈为 `POST /api/retrieval/feedback` |
| 测试 | ✅ | test_bm25_retriever、test_rrf_fusion、test_nli_detector、test_verify_pipeline、test_v2_e2e 等 |
| 配置与说明 | ✅ | .env 已升级为 V2.0 版本，含检索模式、RRF、自适应深度、去噪、质量保障、LLM 任务覆盖等说明 |

---

## 六、已完成的修复与配置要点

- **retrieval_log 模型**：已补充 `confidence_level`、`has_hallucination`、`retrieval_mode`、`citation_accuracy`、`latency_breakdown`，与迁移 V2.0_006 一致。
- **Kimi Provider**：`app/llm/kimi.py` 已存在，`build_chat_provider(provider="kimi")` 支持，temperature 固定 1.0。
- **.env**：V2.0 版本说明完整，包含检索、质量保障、LLM 及可选 `LLM_TASK_OVERRIDES`（如 query_expansion 用不同模型）。
- **Feedback API**：实际路径为 `POST /api/retrieval/feedback`（与 PRD 中可能出现的 `/api/qa/feedback` 不一致，以实现为准；建议 PRD/TRD 统一为 `/api/retrieval/feedback`）。
- **pg_jieba**：使用预构建镜像 `sdrzlyz/pg-ts-jieba:16`，无需自建 Dockerfile。

---

## 七、总体结论与建议

### 7.1 设计完备度

- **18/20 模块**：TRD 覆盖完备（基础设施 3、检索引擎 9、质量保障 5、前端基础 3）。
- **2/20 模块**：存在小缺口（F-2 布局清单可细化，但不影响核心功能）。
- **横切项全部完成**：
  - ✅ TRD 附录十七：端到端流程图（qa_service 串联）
  - ✅ TRD 附录十八：反馈 API 与 V1 表关系
  - ✅ TRD 附录十九：向量检索器接口约定
  - ✅ TRD 4.16 节：document_tasks 串联设计

### 7.2 实现进度

- ✅ 后端骨架、迁移、核心 API、Kimi、.env、retrieval_log 与反馈表扩展均已完成
- ✅ 前端 **QASettingsSheet** 策略联动已实现（`expansion_enabled=false` 时禁用扩展选择器并提示）
- ✅ 策略定义 bug 已修复（`get_strategy()` 默认返回 `smart` 而非不存在的 `default`）

### 7.3 V2.0 L2.5 能力评定

| 评定项 | 结果 |
|--------|------|
| **目标等级** | L2.5 |
| **实际达成** | **✅ L2.5** |
| **核心能力完成度** | 100% |
| **文档完备度** | 98% |
| **可发布状态** | **可发布** |

### 7.4 后续建议

1. **可选优化**：TRD 中补充 F-2 布局组件清单、kimi_provider 小节（低优先级）
2. **V2.1 规划**：意图分析、查询改写、场景适配、离线评估等 L2.7 能力

---

## 九、变更记录

| 日期 | 版本 | 变更内容 |
|------|------|----------|
| 2026-02-23 | v1.0 | 初始版本 |
| 2026-03-01 | v1.1 | 更新模块进度（R-4、V-5、F-3、F-4 已完成）；横切项状态更新；新增 L2.5 能力评定；修复策略默认值 bug |

---

**文档与代码索引**

| 内容 | 文档/位置 |
|------|-----------|
| 产品需求与验收 | `V2升级/V2.0_PRD_检索与质量.md` |
| 技术方案与模块 | `V2升级/V2.0_TRD_检索与质量.md` |
| 能力等级定义 | `V2升级/RAG系统能力三级模型_L1_L2_L3.md` |
| 检索与配置、联动逻辑 | `docs/V2.0_检索与配置汇总.md` |
| 开发流程与产出 | `开发全流程.md` |
| 策略定义 | `backend/app/rag/retrieval_strategy.py` |
| 问答与扩展逻辑 | `backend/app/services/qa_service.py` |
| 策略 API | `backend/app/api/qa.py` → `GET /api/qa/strategies` |
| 反馈 API | `backend/app/api/retrieval.py` → `POST /api/retrieval/feedback` |
| 前端设置项与联动 | `frontend_spa/src/components/qa/QASettingsSheet.tsx` |
| 前端类型与 QA API | `frontend_spa/src/api/types.ts`、`frontend_spa/src/api/qa.ts` |
