# 整改报告

- **版本/分支**：
- **编制人**：
- **日期**：
- **基于验收结论**：

---

## 一、问题列表

| 序号 | 问题描述 | 根因 | 整改措施 | 验证方式 | 状态 |
|------|----------|------|----------|----------|------|
| 1 | 文档上传 400 错误 | 待排查：请求格式/代理/后端校验 | 查看后端日志；核对 multipart 字段名 | 上传 TXT/PDF/DOCX/PNG 验证 | 待验证 |
| 2 | 所有格式均不支持 | 同上，与问题 1 可能同源 | 同上 | 同上 | 待验证 |
| 3 | 最大文件大小不一致 | 前端文案曾为 50MB，与配置不符 | 已统一为 200MB（config、Streamlit、前端文案） | 文案显示 200MB，上传 <200MB 通过 | 已完成 |
| 4 | URL 导入无法解析 | Worker 未启动或抓取失败 | 确保 Redis + Celery Worker 启动 | URL 导入后状态变为 vectorized | 待验证 |
| 5 | URL 文档点击「重新解析」返回「文件不存在」 | reparse() 未区分 URL 类型，将 file_path 当作本地路径 | 在 reparse() 中增加 file_type=="url" 分支 | 对 URL 文档点击重新解析可成功 | 待实施 |

---

## 二、配置与代码变更清单

| 变更项 | 文件 | 说明 |
|--------|------|------|
| 最大文件大小 | backend/app/config.py | max_file_size_mb = 200 |
| 最大文件大小 | frontend/.streamlit/config.toml | maxUploadSize = 200 |
| 前端文案 | frontend/pages/4_文档上传.py | 「最大 200MB」 |
| reparse 支持 URL | backend/app/services/document_service.py | 待实施 |

---

## 三、回归验证结果

- [ ] 单元测试通过
- [ ] 文档上传（多格式）通过
- [ ] URL 导入与重新解析通过
- [ ] 人工验收清单全部勾选

---

**备注：**
