# D4.1 修复 LLM Query Expansion 对接

## 问题描述

`qa_service.py` 中调用 `expand_query()` 时未传递 `llm_provider` 参数，导致 LLM 版 Query Expansion 不工作。

## 需要修改的位置

### 位置 1: `ask()` 方法 (约第 235-239 行)

**原代码：**
```python
        retrieval_start = time.perf_counter()
        queries = [question]
        if use_expansion:
            expansion_mode = getattr(settings, "retrieval_query_expansion_mode", "rule")
            queries = expand_query(question, mode=expansion_mode, max_extra=2) or [question]
```

**修改后：**
```python
        retrieval_start = time.perf_counter()
        queries = [question]
        if use_expansion:
            expansion_mode = getattr(settings, "retrieval_query_expansion_mode", "rule")
            # D4.1: 修复 LLM Query Expansion 对接
            expansion_llm = None
            if expansion_mode in ("llm", "hybrid"):
                try:
                    expansion_llm = get_provider_for_task("qa")
                except Exception as e:
                    logger.warning("LLM provider not available for query expansion: %s", e)
            queries = expand_query(
                question,
                mode=expansion_mode,
                llm_provider=expansion_llm,
                max_extra=2
            ) or [question]
```

### 位置 2: `stream_ask()` 方法 (约第 421-425 行)

**原代码：**
```python
        retrieval_start = time.perf_counter()
        queries = [question]
        if use_expansion:
            expansion_mode = getattr(settings, "retrieval_query_expansion_mode", "rule")
            queries = expand_query(question, mode=expansion_mode, max_extra=2) or [question]
```

**修改后：** (同上)

## 执行修复

由于文件行数较多，请手动执行以下步骤：

1. 打开 `backend/app/services/qa_service.py`
2. 搜索 `expand_query(question, mode=expansion_mode, max_extra=2)`
3. 将其替换为上述修改后的代码
4. 保存文件

或者运行修复脚本：
```bash
python scripts/fix_llm_query_expansion.py
```

## 验证

修复后，当配置 `retrieval_query_expansion_mode: "llm"` 或 `"hybrid"` 时，系统应能正常进行 LLM 版查询扩展。

验证方法：
1. 在 `.env` 中设置 `RETRIEVAL_QUERY_EXPANSION_MODE=llm`
2. 重启后端
3. 进行问答测试
4. 查看日志中是否有 "LLM query expansion" 相关输出
