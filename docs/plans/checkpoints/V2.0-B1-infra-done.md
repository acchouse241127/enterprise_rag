# V2.0 B1 基础设施完成检查点

**完成时间**：2026-02-27  
**Git SHA**：`git rev-parse HEAD` 待提交后更新

---

## 验收命令输出

### pg_jieba 扩展
```
CREATE EXTENSION
```

### chunks 表
```
\d chunks
                                  Table "public.chunks"
      Column       |           Type           | Collation | Nullable |      Default      
-------------------+--------------------------+-----------+----------+-------------------
 id                | uuid                     |           | not null | gen_random_uuid()
 document_id       | integer                  |           | not null | 
 knowledge_base_id | integer                  |           | not null | 
 collection_name   | character varying(200)   |           | not null | 
 chunk_index       | integer                  |           | not null | 
 content           | text                     |           | not null | 
 content_tsv       | tsvector                 |           |          | 
 chunk_mode        | character varying(30)    |           |          | 
 token_count       | integer                  |           |          | 
 section_title     | character varying(500)   |           |          | 
 parent_chunk_id   | uuid                     |           |          | 
 is_parent         | boolean                  |           |          | false
 metadata          | jsonb                    |           |          | '{}'::jsonb
 created_at        | timestamp with time zone |           |          | now()
Indexes:
    "idx_chunks_content_tsv" gin (content_tsv) WHERE content_tsv IS NOT NULL
    "idx_chunks_kb_id" btree (knowledge_base_id)
    ...
Triggers:
    trg_chunks_tsv BEFORE INSERT OR UPDATE ON chunks FOR EACH ROW EXECUTE FUNCTION chunks_tsv_trigger()
```

### knowledge_bases 表
```
chunk_mode            | character varying(30)  | default 'chinese_recursive'
parent_retrieval_mode | character varying(20)  | default 'dynamic'
dynamic_expand_n      | integer                | default 2
Check constraints:
    check_chunk_mode, check_parent_retrieval_mode, check_dynamic_expand_n
```

---

## 完成项

- [x] B1-1：PostgreSQL 自定义镜像 + pg_jieba
- [x] B1-2：chunks 表 + P0/P1 索引 + content_tsv 触发器
- [x] B1-3：knowledge_bases 表迁移

---

## 下一批任务

**B1-4 ~ B1-8**：检索层改造（B1-retrieval）

详见 `docs/plans/V2.0-B1-retrieval.md`。
